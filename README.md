# Texas-TCEQ-Violations-and-Compliance-Analytics-Pipeline
The overall workflow is an end-to-end ETL and analytics pipeline that starts with raw TCEQ violation and compliance files, standardizes them, stores them in databases, merges them into an analysis-ready dataset, and prepares the final output for dashboard reporting.
